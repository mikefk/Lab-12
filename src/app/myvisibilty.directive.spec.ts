import { MyvisibiltyDirective } from './myvisibilty.directive';

describe('MyvisibiltyDirective', () => {
  it('should create an instance', () => {
    const directive = new MyvisibiltyDirective();
    expect(directive).toBeTruthy();
  });
});
